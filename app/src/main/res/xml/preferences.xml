<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!--<CheckBoxPreference-->
        <!--android:key="pref_sync"-->
        <!--android:title="Enable"-->
        <!--android:summary="starte Action"-->
        <!--android:defaultValue="true" />-->
    <ListPreference
        android:key="filter"
        android:defaultValue="Alle"
        android:title="Bewertungs Filter"
        android:entries="@array/filter_entries"
        android:entryValues="@array/filter_entries"/>

    <ListPreference
        android:key="sort"
        android:defaultValue="Locationbewertung"
        android:title="Bewertungs Sortierung"
        android:entries="@array/sort_entries"
        android:entryValues="@array/sort_entries"/>
</PreferenceScreen>